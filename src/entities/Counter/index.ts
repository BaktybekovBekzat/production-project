import { Counter } from './ui/Counter'
import { ICounterSchema } from './model/types/counterSchema'
import { counterReducer } from './model/slice/counterSlice'

export { Counter, counterReducer, ICounterSchema }
